### REQ-007: Prefix grouping — shared access scope for co-deployed courses and documents

- **Status:** OPEN
- **Added:** 2026-04-13
- **Completed:** –
- **Verified by:** –
- **Depends on:** REQ-005, REQ-006
- **Description:**

**Problem:** A course and one or more companion documents are logically related
and contain internal cross-links. Deploying them as separate top-level paths
(e.g. `/digital-und-mikrocomputertechnik/` and `/esp-survival-guide/`) means
each needs its own token. The Worker issues cookies scoped to the first URL
segment, so a cookie for one path does not cover the other. Cross-document
links fail for students who received only one token or who have not yet visited
both entry points.

**Solution:** Introduce an optional `prefix` field on `projects.yml` entries.
Prefixed projects are deployed under `<prefix>/<name>/` instead of `<name>/`.
A single token issued for the prefix covers every project beneath it. Tokens
may also be issued for a fully-qualified path (`<prefix>/<name>`) to grant
access to one specific document within a group.

The Cloudflare Worker is updated to perform **scope-based path-prefix
matching** instead of first-segment matching, making both token granularities
work correctly without breaking existing tokens.

---

### 1. `projects.yml` schema change

Add an optional `prefix` field to project entries:

```yaml
projects:
  - name: digital-und-mikrocomputertechnik
    type: course
    prefix: mk4-26

  - name: esp-survival-guide
    type: doc
    prefix: mk4-26

  - name: unrelated-standalone-doc
    type: doc
    # no prefix — behaviour unchanged
```

The `prefix` value must satisfy the same regex as `name`: `[a-z0-9][a-z0-9._-]*`.
There is no separate manifest entry for the prefix itself; it is just a
shared label on existing entries.

---

### 2. `matctl course add` / `matctl doc add` — `--prefix` flag

Both scaffold commands gain an optional `--prefix` flag:

```bash
matctl course add digital-und-mikrocomputertechnik --prefix mk4-26
matctl doc add esp-survival-guide --prefix mk4-26
```

When supplied, `prefix: <value>` is written into the manifest entry alongside
`name` and `type`. Without `--prefix` the entry is identical to today.

---

### 3. Cloudflare Worker — scope-based authorization

**Current behaviour:** `course` = first path segment; `isAuthorized` checks
exact equality against `record.course`.

**New behaviour:** the token's `course` field is treated as a path-scope prefix
of arbitrary depth. Authorization passes when the request pathname starts with
`/<scope>/` (or equals `/<scope>`):

```js
function isAuthorized(record, pathname) {
  if (record.course === "*") return true;
  const prefix = "/" + record.course;
  return pathname === prefix
      || pathname.startsWith(prefix + "/");
}
```

The **session cookie** stores the token's scope verbatim (not the first
segment). `makeSessionCookie` receives the scope string from the token record;
`verifySessionCookie` checks that the request pathname is covered by the
cookie's stored scope using the same prefix test.

**Backward compatibility:** existing tokens with `course:
"digital-und-mikrocomputertechnik"` satisfy `isAuthorized` for any path
starting with `/digital-und-mikrocomputertechnik/` — identical to today's
behaviour.

**Token granularities after this change:**

| Token `course` value | Covers |
|---|---|
| `mk4-26` | all of `/mk4-26/**` |
| `mk4-26/digital-und-mikrocomputertechnik` | only `/mk4-26/digital-und-mikrocomputertechnik/**` |
| `digital-und-mikrocomputertechnik` | `/digital-und-mikrocomputertechnik/**` (existing, unchanged) |
| `*` | everything (existing wildcard, unchanged) |

---

### 4. `matctl token issue` — iLearn URL derivation

No change to the KV write or the argument interface. The iLearn URL is derived
by splitting `course` on `/` and joining:

- `mk4-26` → `https://material.professorfroehlich.de/mk4-26/?token=...`
- `mk4-26/digital-und-mikrocomputertechnik` → `…/mk4-26/digital-und-mikrocomputertechnik/?token=...`
- `*` → `https://material.professorfroehlich.de/?token=...` (unchanged)

The existing special-case for `*` is retained; all other values use the generic
path-join rule.

---

### 5. CI `publish.yml` (in `material` repo) — deploy target

When a project has a `prefix`, the deploy target changes:

| `prefix` | Deploy target on Netcup |
|---|---|
| absent | `httpdocs/<name>/` (unchanged) |
| `mk4-26` | `httpdocs/mk4-26/<name>/` |

The workflow must:

1. Read the `prefix` field from `projects.yml` during matrix expansion.
2. Compute `DEPLOY_DIR = <prefix>/<name>` when prefix is set, else `<name>`.
3. Use `ssh mkdir -p httpdocs/<DEPLOY_DIR>/` before the `scp` step (the
   existing `rm -rf` + recreate pattern applies to `httpdocs/<DEPLOY_DIR>/`).
4. Deploy rendered output to `httpdocs/<DEPLOY_DIR>/` using the same rules as
   today (book output for `course`, flat output for `doc`).

Change detection is unaffected: project source files still live under `<name>/`
in the repository.

---

### 6. `matctl remove` — updated cleanup note

When a project has a prefix, the post-remove note updates the remote path from
`material.professorfroehlich.de/<name>/` to
`material.professorfroehlich.de/<prefix>/<name>/`.

---

### 7. Documentation updates

- `docs/administration.md §5.1` — document `prefix` field in the manifest
  schema table.
- `docs/administration.md §8.1` — document fully-qualified scope tokens with
  an example (`mk4-26/digital-und-mikrocomputertechnik`).
- `docs/administration.md` architecture/deploy sections — update path examples
  where relevant.

---

- **Acceptance criteria:**
  - [ ] `matctl doc add esp-survival-guide --prefix mk4-26` writes
        `{name: esp-survival-guide, type: doc, prefix: mk4-26}` to
        `projects.yml`
  - [ ] `matctl course add digital-und-mikrocomputertechnik --prefix mk4-26`
        writes the corresponding course entry with `prefix: mk4-26`
  - [ ] Projects without `prefix` produce manifest entries identical to today
  - [ ] `matctl token issue mk4-26 "WS2026"` prints iLearn URL
        `…/mk4-26/?token=...`
  - [ ] `matctl token issue mk4-26/digital-und-mikrocomputertechnik "Person X"`
        prints iLearn URL `…/mk4-26/digital-und-mikrocomputertechnik/?token=...`
  - [ ] Worker: a token with `course: "mk4-26"` grants access to both
        `/mk4-26/digital-und-mikrocomputertechnik/` and
        `/mk4-26/esp-survival-guide/` in a live browser test
  - [ ] Worker: a token with `course: "mk4-26/esp-survival-guide"` grants
        access to `/mk4-26/esp-survival-guide/` but returns 403 for
        `/mk4-26/digital-und-mikrocomputertechnik/`
  - [ ] Worker: existing token with `course: "digital-und-mikrocomputertechnik"`
        (no prefix, root-level) continues to work unchanged
  - [ ] Worker: `course: "*"` wildcard token continues to work unchanged
  - [ ] CI deploys a prefixed project to `httpdocs/mk4-26/<name>/` on Netcup
  - [ ] CI deploys an unprefixed project to `httpdocs/<name>/` unchanged
  - [ ] Cross-document link from `digital-und-mikrocomputertechnik` to
        `esp-survival-guide` navigates successfully for a student who entered
        via the `mk4-26` token URL (single cookie, no second token needed)
  - [ ] `docs/administration.md` updated per §7

- **Notes:**

- The `prefix` field is a deploy-time concept only. Quarto renders projects
  into `<name>/_output/...` as today; the prefix is invisible to Quarto.
- Cross-document links in content must be written as relative paths
  (`../esp-survival-guide/`) or absolute URLs. The prefix makes the cookie
  cover both paths; it does not rewrite links automatically.
- A future requirement (file browser at `<prefix>/index.html`) fits naturally
  into this model: a static page deployed to `httpdocs/mk4-26/` is covered
  by the same `mk4-26` token with no further Worker changes.
- The Worker update changes cookie internals (scope stored verbatim rather than
  derived from the first segment). Students with pre-existing cookies for
  root-level courses (e.g. `digital-und-mikrocomputertechnik`) will need to
  re-authenticate once after the Worker is redeployed. This is acceptable;
  document it in the deployment note for this requirement.
- No KV schema migration: the `course` field in existing token records is
  already a plain string. New values (`mk4-26`, `mk4-26/foo`) are just
  strings with a slash — no structural change.
